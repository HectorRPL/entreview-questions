// reverse-linked-list mention the pointer of nodes
// [init 1 end ] ->[init 3 end ]->4->7->9
// consider the value and the pointer
// => 9 7 4 3 1

const arr = [1, 3, 4, 7, 9]

class Node {
    constructor(value, {ini, end}) {
        this.value = value
        this.pointer = {ini, end}
    }
}

// const node = new Node()
let result = []

for (let i = arr.length; i > arr.length; i--) {
    const node = new Node(arr[i], )
}




