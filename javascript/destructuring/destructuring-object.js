const happyMoments = {
    daf: 'first love',
    gaby: 'second love',
    loretto: 'only fuck',
}

const {
    girlOne,
    gaby,
    loretto = '=)',
    nayeli = 'would be nice'
} = happyMoments

console.log({girlOne, gaby, loretto, nayeli});
