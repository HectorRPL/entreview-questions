/*
What is the difference between the map()
and the forEach() methods on Array prototype?

a)
The forEach() method returns null, whereas
the map() method performs operations on each
value in the array.

b)
There is no difference

c)
The map() method returns a new array with a
transformation applied on each item in the
original array, whereas the forEach() method
iterates through an array with no return value.

d)
The forEach() method returns a new array with
a transformation applied to each item in the
original array, whereas the map() method
iterates through an array with no return value

*/
